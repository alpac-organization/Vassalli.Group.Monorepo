import { Fragment } from "react"

export const SelectCompany = function (): JSX.Element {
    return (
        <Fragment>
            <div style={{ display: "flex", gap: 20 }}>
                Selecciona tu empresa (Catálogo)
            </div>
        </Fragment>
    )
}