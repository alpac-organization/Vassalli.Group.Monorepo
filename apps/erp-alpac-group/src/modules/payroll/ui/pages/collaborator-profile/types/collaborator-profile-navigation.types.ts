/**
 * Estado opcional pasado por react-router al navegar al perfil (p. ej. desde la lista de colaboradores).
 */
export type CollaboratorProfileLocationState = {
  identification_number?: string;
};
