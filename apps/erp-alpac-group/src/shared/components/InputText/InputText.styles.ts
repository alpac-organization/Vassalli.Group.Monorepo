import type { CSSProperties } from "react"

export const getInputTextStyles = (): CSSProperties => {
    return {
        outline: "none",
        padding: "10px 16px",
        borderRadius: "6px"
    }
}