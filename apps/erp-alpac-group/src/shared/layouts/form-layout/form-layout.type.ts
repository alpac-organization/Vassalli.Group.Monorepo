import type { ReactNode } from "react";

export type FormLayoutProps = {
    title?:                string,
    imageUrl?:             string,
    copyrightEntityName?: string,
    children?:             ReactNode,
}